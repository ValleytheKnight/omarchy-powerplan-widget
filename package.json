{
  "name": "omarchy-powerplan",
  "version": "0.1.0",
  "private": true,
  "description": "Set lid-close actions, screensaver, lock, sleep, hibernate, and power profile, independently for plugged in vs on battery.",
  "scripts": {
    "test": "node --test && python3 -m unittest discover -s test -p 'test_*.py'"
  },
  "license": "MIT"
}
